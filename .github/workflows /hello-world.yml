name: Hello World

on: push

jobs:
  hello-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: List and Read file
        run: |
          echo "Hello, world!"
